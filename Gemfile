# Gemfile for local Jekyll development only
source "https://rubygems.org"

# Pull in Jekyll itself (latest 4.x should work fine locally)
#gem "jekyll", "~> 4.3"
gem "github-pages", group: :jekyll_plugins

# Ruby 3.x and later requires webrick for `jekyll serve`
gem "webrick", "~> 1.8"

